# Copy this to .env.test and fill in your values, or export directly in your shell.
# These are only needed for authenticated E2E tests (realms, workspace, subscription).

TEST_EMAIL=your-test-account@example.com
TEST_PASSWORD=your-test-password
